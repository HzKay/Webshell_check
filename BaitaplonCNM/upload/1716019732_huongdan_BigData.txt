//// LAB 2
// Xem địa chỉ ip của máy ảo
ifconfig -a (ip may ao)

// Kết nối cho máy thực và máy ảo thấy nhau
ping ip_may_ao

// Sao chép file từ máy thật qua máy ảo 
scp ten_file hduser@ip_may_ao:/home/hduser
vd: scp thungan.csv hduser@192.168.1.180:/home/hduser

Nhập password để xác thực: hadoop

// Xem đường dẫn hiện hành 
pwd


//// LAB 3
// Khởi tạo Hadoop v2.6
start-dfs.sh

// Khởi tạo yarn
start-yarn.sh

// Kiểm tra tình trạng của hệ thống hadoop (Không quan trọng)
hdfs fsck / 

// Xem đường dẫn cài đặt thư mục Hadoop (Không quan trọng)
echo $HADOOP_HOME 

// Liệt kê danh sách tập tin và thư mục 
hdfs dfs -ls / 

// Tạo thư mục mới
hdfs dfs -mkdir /ten_thu_muc 
VD: hdfs dfs -mkdir /thungan

// Tạo tập tin trong thư mục 
hdfs dfs -touchz /thungan/test.txt
// Kiêm tra tập tin trong thư mục vừa tạo 
hdfs dfs -ls / thungan

// Kiểm tra kích thước tập tin vừa tạo 
hdfs dfs -du -s /thungan/test.txt

// Tạo thư mục (Local-Linux)
mkdir ten_thu_muc

// Tạo mới tệp tin chứa nội dung 
cat > ten_thu_muc/ten_tep_tin.txt
Noi_dung
Noi_dung 
Ctrl D

// Xem nội dung trong tệp tin 
cat ten_thu_muc/ten_tep_tin

//Đưa dữ liệu từ Local lên HDFS. 
hdfs dfs -copyFromLocal file_name(local) ten_thu_muc(hdfs)
VD: hdfs dfs –copyFromLocal /home/hduser/test/data.txt /mrnam

// Đọc nội dung và in nội dung của file ra màn hình
hdfs dfs -cat /thungan/data.txt

// Lấy nội dung của tập tin và xuất ra dạng text
hdfs dfs –text /mrnam/data.txt

// (HDFS) Đếm số lượng thư mục, tập tin và dung lượng của đường dẫn
hdfs dfs –count /mrnam

//(HDFS) Xóa tập tin trên HDFS
hdfs dfs –rm /mrnam/sample.txt

//(HDFS) Sao chép tập tin đến đường dẫn lưu trữ HDFS.
hdfs dfs -cp /CEnetBig /mrnam

//(HDFS) Di chuyển tập tin đến đường dẫn lưu trữ HDFS. 
Lưu ý: Tạo thư mục newmrnam trước
hdfs dfs -mv /mrnam/sample.txt /newmrnam

//Xóa thư mục trên hdfs
hdfs dfs -rm -r /ten_thu_muc